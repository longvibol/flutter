// ignore_for_file: unused_local_variable, duplicate_ignore

void main() {
  String txt = "true";
  // bool p = txt == "true" ? true :false;

  if (txt == "true") {
    bool p = true;
    print(p);
  } else {
    bool p = false;
    print(p);
  }
}
